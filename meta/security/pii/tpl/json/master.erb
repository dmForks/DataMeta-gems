{
<% flds = reg.keyVos.keys.sort; flds.each_with_index { |kf, fix| fldName = kf.to_s.inspect; fldDef = reg.keyVos[kf] %>
    <%= fldName %>: {<% attrs = fldDef.attrs.keys.sort; attrs.each_with_index {|ka, aix|  keyStr = ka.to_s.inspect; valStr = fldDef.attrs[ka].inspect %>
       <%=keyStr%>: <%= valStr %><%=aix == attrs.size - 1 ? '' : ','%><%}%>
    }<%=fix == flds.size - 1 ? '' : ','%>
<% } %>
}